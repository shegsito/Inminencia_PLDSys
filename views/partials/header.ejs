<header class = "top">
    <h1>Sistema de monitoreo</h1>

    <% if (typeof buttonText !== 'undefined') { %>
        <button class = "top-right"><%= buttonText %>
        </button>
    <% }  %>
</header>